source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in enum_timestamps.gemspec.
gemspec

group :development do
  gem 'pg', "~> 1.2.3"
  gem 'pessimize', "~> 0.4.0"
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
